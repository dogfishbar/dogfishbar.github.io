const AWS = require('aws-sdk');
const dynatable = require('./dist');

AWS.config.update({
  region: "eu-west-1",
  accessKeyId: "AKIAIQHMJST5EVAW53QA",
  secretAccessKey: "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"
});
// aws_access_key_id=AKIAJX24PKMIEKUZBY3Q
// aws_secret_access_key=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
const docClient = new AWS.DynamoDB.DocumentClient();

// Imagine you have a DynamoDB table called `users`
const users = dynatable(docClient, 'w00t', { id: 'N' });
// users.findOne({ name: 'Nisse' })

// users.findOne({name: { $in: ['Nisse'] }})
//   .then(users => console.log(users));

users.update({id: 11}, { name: 'Nisse2' })
  .then(res => console.log(res))
  .catch(err => console.log(err));

// users.put({
//   id: 11,
//   name: 'Nisse',
//   added: new Date(),
//   interests: ['cars', 'farts']
// });
